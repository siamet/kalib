[pytest]
# Collect only the test suite. The repository root also holds
# test_camera_connection.py and test_pi_connection.py, which are hardware
# diagnostics run by hand against the instrument, not pytest tests: their
# test_connection() takes a positional argument pytest reads as a fixture,
# so bare `pytest` collected them and errored while `pytest tests/` passed.
testpaths = tests
